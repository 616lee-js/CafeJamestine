# Supabase — from dashboard → Project Settings → API
# Public (sent to browser):
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — NEVER expose to the client / commit:
SUPABASE_SERVICE_ROLE_KEY=
