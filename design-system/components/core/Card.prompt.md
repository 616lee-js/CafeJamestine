Card is for grouped, framed content (a bag, a summary panel). For lists the app uses `ListRow`, not Card.

```jsx
<Card>
  <CardHeader title="Ethiopia Guji" description="Onyx · washed" action={<Badge status="resting">resting</Badge>} />
  <CardContent>…</CardContent>
</Card>
```
