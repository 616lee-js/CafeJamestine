Bottom-right transient. In Café Jamestine toasts report failure only — saves are silent because every field autosaves.

```jsx
<Toast kind="error" title="Save failed: network offline" />
```

Copy pattern: `"Save failed: {message}"`, `"Delete failed: {message}"`, `"Upload failed: {message}"`.
