A single muted sentence in place of the list — no icon, no border, no button.

```jsx
<EmptyState>No active sessions.</EmptyState>
<EmptyState>No coffees have an Active bag. Set a bag to Active on a coffee first.</EmptyState>
```

Pattern: state the absence, then (only if there's a required next step) name it plainly.
