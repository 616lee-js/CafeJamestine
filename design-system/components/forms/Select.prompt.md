For closed reference lists. Open, user-extensible lists use `Combobox` instead.

```jsx
<Select size="touch" options={[{value:"__none__",label:"— None —"},{value:"v60",label:"V60"}]} />
```

The nullable option is literally `— None —` (em dashes, spaced).
